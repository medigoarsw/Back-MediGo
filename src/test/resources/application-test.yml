spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/medigo_test
    driver-class-name: org.postgresql.Driver
    username: medigo
    password: medigo123
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379
  liquibase:
    enabled: false
  security:
    enabled: false
  main:
    allow-bean-definition-overriding: true